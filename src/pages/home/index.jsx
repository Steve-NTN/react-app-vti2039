import styled from "styled-components";
import { Header } from "../../components";

const Home = () => {
  return (
    <StyledHome>
      <Header />
    </StyledHome>
  );
};

const StyledHome = styled.div``;

export default Home;
